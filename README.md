# SilkOS Kernel, a simple shell

### This is a simple bootable kernel for QEMU+shell commands.

### Run help in QEMU shell to see a list of commands.

# Run instructions

### In . run
#### make run

# Or, if you want an iso,
#### make iso
